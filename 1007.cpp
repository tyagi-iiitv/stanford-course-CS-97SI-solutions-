#include <iostream>
using namespace std;

void solve(string array[], int m){
	int inversion_factor[m];
	for()
	for(int i=0;i<m;i++){
		for(int j=i+1;j<m;j++){
			if(array[i][j])
		}
	}
}

int main(){
	ios::sync_with_stdio(false);
	int n, m;
	cin>>n>>m;
	string array[m];
	while(m){
		getline(cin, array[m-1]);
	}	
	solve(array, m);
	return 0;
}